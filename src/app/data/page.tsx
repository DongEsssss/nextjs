import DataPage from '@/src/components/pages/data/page';
import React from 'react';

// 클라이언트 컴포넌트를 사용하는 경우 "use client" 추가
const Data = () => {
    return (
        <DataPage />
    );
};

export default Data;
